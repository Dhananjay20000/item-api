# Server Configuration
# Use PORT environment variable if available (for cloud platforms), otherwise default to 8080
server.port=${PORT:8080}
server.servlet.context-path=/

# Application Configuration
spring.application.name=Item Management API

# Logging Configuration
logging.level.com.example.itemapi=INFO
logging.level.org.springframework.web=INFO

